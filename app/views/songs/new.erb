<!DOCTYPE html>
<html lang="en">
  <head>
    <title>SONG LISTR - Add New Song</title>
    <!-- Bootstrap core CSS -->
    <link href="/css/bootstrap.min.css" rel="stylesheet">
  </head>
  <body>
    <div class="container-fluid">
      <div class="page-header">
        <h1>SONG LISTR <small>Add New Song</small></h1>
      </div>
      <form method="post" action="/songs">
        <div class="form-group<% if @song.errors[:author].any? %> has-error<% end %>">
          <input type="text" name="author" class="form-control" placeholder="Name" value="<%= @song.author %>">
<% if @song.errors[:author].any? %>
          <span class="help-block"><%= @song.errors.full_messages_for(:author) %></span>
<% end %>
        </div>
        <div class="form-group<% if @song.errors[:title].any? %> has-error<% end %>">
          <input type="text" name="title" class="form-control" placeholder="Song Title" value="<%= @song.title %>">
<% if @song.errors[:title].any? %>
          <span class="help-block"><%= @song.errors.full_messages_for(:title) %></span>
<% end %>
        </div>
        <div class="form-group">
          <input type="text" name="url" class="form-control" placeholder="URL" value="<%= @song.url %>">
        </div>
        <div>
          <button type="submit" class="btn btn-primary">Submit</button>
        </div>
      </form>
    </div>
  </body>
</html>