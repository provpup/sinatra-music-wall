<!DOCTYPE html>
<html lang="en">
  <head>
    <title>SONG LISTR - Top requested songs</title>
    <!-- Bootstrap core CSS -->
    <link href="/css/bootstrap.min.css" rel="stylesheet">
  </head>
  <body>
    <div class="container-fluid">
      <div class="page-header">
        <h1>SONG LISTR <small>Top requested songs</small></h1>
<% if @user.id %>
        <p class="lead"><a href="/songs/new">Post a song... It's FREE (for now)</a></p>
        <p><a href="/logout" class="btn btn-info" role="button">Logout</a></p>
<% end %>
      </div>
      <div class="table-responsive">
      <table class="table table-striped">
        <thead>
          <tr>
            <th></th>
            <th>Song Title</th>
            <th>Submission Author</th>
            <th>URL</th>
          </tr>
        </thead>
        <tbody>
          <% @songs.each do |song| %>
          <tr>
            <td class="col-md-2 text-center">(<%= song.upvotes.count %>)
<% if @user.id && !@user.upvoted_song?(song) %>
              <a href="/upvote?song=<%= song.id%>"><span class="glyphicon glyphicon-thumbs-up"></span></a>
<% elsif @user.upvoted_song?(song) %>
              Upvoted
<% end %>
            </td>
            <td class="col-md-3"><a href="/songs/<%= song.id %>"><%= song.title %></a></td>
            <td class="col-md-3"><%= song.author %></td>
            <td class="col-md-6"><a href="<%= song.url %>"><%= song.url %></a></td>
          </tr>
          <% end %>
        </tbody>
      </table>
      </div>
    </div>
  </body>
</html>