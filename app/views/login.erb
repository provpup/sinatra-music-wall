<!DOCTYPE html>
<html lang="en">
  <head>
    <title>SONG LISTR - User Login</title>
    <!-- Bootstrap core CSS -->
    <link href="/css/bootstrap.min.css" rel="stylesheet">
  </head>
  <body>
    <div class="container-fluid">
      <div class="page-header">
        <h1>SONG LISTR <small>User Login</small></h1>
      </div>
      <form method="post" action="/login">
        <div class="form-group<% if @user.errors[:email].any? %> has-error<% end %>">
          <input type="text" name="email" class="form-control" placeholder="E-mail" value="<%= @user.email %>">
<% if @user.errors[:email].any? %>
          <span class="help-block"><%= @user.errors.full_messages_for(:email) %></span>
<% end %>
        </div>
        <div class="form-group<% if @user.errors[:email].any? %> has-error<% end %>">
          <input type="password" name="password" class="form-control" placeholder="Password" value="<%= @user.password %>">
<% if @user.errors[:email].any? %>
          <span class="help-block"><%= @user.errors.full_messages_for(:password) %></span>
<% end %>
        </div>
        <div>
          <button type="submit" class="btn btn-primary">Login</button>
        </div>
      </form>
    </div>
  </body>
</html>